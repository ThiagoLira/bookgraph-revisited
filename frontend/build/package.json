{
  "name": "bookgraph-frontend-build",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Offline bake step: enriches stage-3 BookGraph JSON with nationality regions, author photos, and a fixed pre-computed layout (baked.json sidecars consumed by the Canvas 2D frontend).",
  "scripts": {
    "bake": "node build.mjs --all",
    "bake:dataset": "node build.mjs --dataset"
  },
  "dependencies": {
    "d3": "^7.9.0"
  }
}
